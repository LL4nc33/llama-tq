# llama-tq roadmap — post Gemma4 unblock

Stand: 2026-04-25. Gemma4 läuft (war test-harness artifact, nicht bug). Beide Modelle (Qwen3.6-35B-A3B, Gemma4-26B-A4B) gebenchmarkt mit voller 21-config K×V matrix.

Zentrale Beobachtung: VTQ_2 Pareto auf Qwen near-lossless (-1.0% PP), aber Gemma4 hat -2.4 bis -6.6% TG slowdown. Hauptbottleneck: V_rows-loop ineffizient bei D=512 (Gemma4 full-attn) → block-header reload waste.

## Phase 1 — V_rows=8 D≥256 fix ✅ DONE (2026-04-25)

Commits: 283eee6f2 (VTQ_2) + 584378082 (VTQ_1 family) + 6c50bc370 (verification report) + 24f3c7eb1 (full README refresh).

**Result:** Gemma4 VTQ-family +2 bis +6% TG (besser als erwartet). Detail-Report in `docs/plans/2026-04-25-phase1-vrows-results.md`.

- f16/vtq2_2 TG: 80.70 → 82.73 (+2.5%)
- ktq2_1/vtq2_2 TG: 77.27 → 79.88 (+3.4%)
- ktq2_1/vtq3_1 TG: 71.92 → 76.44 (+6.3%, best gain)
- Qwen3.6 D=128 unverändert (Fix gilt nur D≥256), Pareto intakt

## Phase 2 — vtq1_1 (1bit) sweep ✅ DONE (2026-04-25)

Commit 24f3c7eb1. Beide Modelle haben jetzt vollständige 50-config Tabelle in README.

**Key findings:**
- **Gemma4 D=512:** `f16/vtq1_1` nur −3.4% TG → 1bit V durchaus praktikabel (Phase 1 fix half mit)
- **Gemma4:** `ktq1_1/vtq1_1` 2.0 bpw avg, nur −7.9% TG → "Aggressive tier" mit minimalem Speed-Hit
- **Qwen3.6 D=128:** `f16/vtq1_1` nur −2.0% TG, `ktq1_1/vtq1_1` −3.2% TG — selbe Pattern
- **vtq4_2 ist neuer top V:** `f16/vtq4_2` auf Gemma4 nur −0.7% PP / −1.4% TG (vs −1.6%/−2.3% bei vtq2_2)
- **VTQ_1 D=512 weiterhin schwach** vs D=128 (`vtq2_1` −25% PP auf Gemma4 vs −6.5% auf Qwen)

Quality-tier-grid:
- **Lossless:** vtq4_2 (PPL +0.02% Qwen), vtq2_2 family
- **Light:** vtq2_1 (PPL +6%)
- **Aggressive:** vtq1_1 (PPL +16%)

## Phase 3 — VTQ_3 family (Outlier-Channel-Split, "_3" Generation)

Neuer block type. Naming-Konvention bestätigt vom user.

Block layout:
```c
typedef struct {
    half       d;
    uint16_t   start_state;
    uint8_t    outlier_idx[K_OUT];                 // K_OUT=4 outlier channels
    half       outlier_vals[K_OUT * SAMPLES_PER];  // FP16 für outlier samples
    uint8_t    qs[QK_VTQ_TRELLIS * K / 8];          // K=2/3/4 für vtq2_3/vtq3_3/vtq4_3
} block_vtq{2,3,4}_3;
```

Bpw rechnung: vtq2_2 (2.25) + ~0.3 overhead = ~2.55 bpw für vtq2_3.

Hypothese: schließt den Gemma4 -6.6% TG gap auf Qwen-level (~-3%) und schließt den PPL gap zu f16 auf <0.5% bei beiden Modellen.

Aufwand-Schätzung:
- Block layout + struct definitions in ggml-common.h: 1h
- Round-trip CPU encoder/decoder + tests: 4h
- CUDA decoder + FA dispatch entries (12 K-types × 3 V-types × 4 D-werte): 1 Tag (template instantiation, build time ~30min/iter)
- Python encoder (calibration + outlier-detection on activations): 4-6h
- README + benchmarks.csv + chart: 2h

Total: 3-5 Tage für eine PR. Hoher Wert.

## Phase 4 — V-RMS-norm-aware VTQ_2 / VTQ_3 calibration

Gemma4 normt V vor KV-write (`gemma4-iswa.cpp:92` `ggml_rms_norm`). Trellis-Codebook ist auf Qwen-V trainiert (nicht-normalisiert). Re-fit der Lloyd-Max centroids auf Gemma4 V-stats sollte den residualen PPL gap schließen.

- Pure Python prep, kein kernel-touch
- Add `tools/calibrate_vtq2.py` (existiert evtl. schon, prüfen)
- Run auf 1k V-samples aus Gemma4 forward
- Generate gemma4-specific codebook table → optional include in build oder via env var
- Bench delta

Aufwand: ~1 Tag. Ergänzt Phase 3 (Outlier nimmt die head-on issues, calib polished den rest).

## Phase 5 — Per-head adaptive bpw

Trick 2 PR1 (profiling hook) ist da (Task #137 completed). Trick 2 PR2 (per-layer mixed precision) ebenfalls (#138). Aber die per-head Logik ist nicht produktivisiert.

Plan:
- Profile Gemma4 + Qwen3.6 head-variance (already supported via `--tq-profile-heads`)
- Persist profile als gguf metadata
- Add `--tq-v-strategy adaptive` der profile.var auf bpw mapped
- High-variance heads → vtq3_3, low-variance → vtq1_1
- Bench

Aufwand: ~1-2 Tage. Marginaler Gewinn nach Phase 3+4 (vermutlich +0.2 bis +0.5% TG, -0.1% PPL) aber relativ billig.

## Phase 6 — Tooling ✅ PARTIAL DONE (2026-04-25)

- ✅ **llama-bench `LLAMA_ARG_TQ_V_OVERRIDE` env var** — commit `78c3ece6d`. Same syntax wie `--tq-v-override`. Resolved nach model-load via `llama_tq::resolve_v_types`. Lever 1 measurement jetzt möglich.
- pending **llama-server bench harness für Gemma4 reasoning models** — llama-cli print kein eval-time bei `<|channel>thought`. Niedrige Priorität (UX).

**Lever 1 result (Gemma4 SWA-mix):**
- f16/vtq2_2 + SWA=f16: 1381.16 PP / 84.81 TG — **+2.8% PP / +2.5% TG vs uniform f16/vtq2_2**
- ktq2_1/vtq2_2 + SWA=f16: 1344.61 PP / 80.89 TG — +2.0% PP / +1.3% TG
- avg_bpw 3.55 (vs 2.25 uniform) — gives back 25% of compression for the speed win

Production-recommended Gemma4 config jetzt `f16/vtq2_2 + SWA=f16` (1381/84.81) — schneller UND kleinere KV als uniform `f16/f16` (1366/84.72).

## Pending non-blocker tasks

- #142 — Disable per-layer mixed V-cache as default (Production-Guard)
- #158 — MMA-KTQ Phase 5: Deploy + docs

## Nicht-Pareto / Rabbit holes (deferred)

- D-aware QK_VTQ_TRELLIS block size (256 statt 128 für D=512) — riesige Refactor, marginal Gain wenn V_rows fix schon wirkt
- VTQ_1 D=512 dedicated kernel rewrite — falls V_rows=8 nicht reicht, sonst skip
- Gemma4 reasoning UX (skip `<|channel>thought` in server output) — UX feature, nicht Performance

## Sequenz (priorisiert)

1. Phase 1 verifikation (heute, blockt auf build)
2. Phase 2 vtq1_1 sweep (1 Tag)
3. Phase 6 tooling (parallel zu Phase 2, 2h)
4. Phase 3 VTQ_3 implementation sprint (3-5 Tage)
5. Phase 4 calibration (1 Tag, parallel zu Phase 3 Python-side)
6. Re-bench beider Modelle mit VTQ_3 → README rewrite, bekanntmachen
7. Phase 5 adaptive bpw (1-2 Tage, nice-to-have)

Erwartetes Endresultat: beide Modelle (Qwen3.6 + Gemma4) bei sub-2% PP/TG slowdown UND ~-0.3% PPL bei vtq2_3/vtq3_3 — production-ready für jeden head_dim.
